import React from 'react';

const Register = () => {
    return (
        <div>
            {/* <form onSubmit={handleRegister}>
      <input onBlur={handleEmailBlur} type="email" id="email" name="email"/><br/>
      <input onBlur={handlePassBlur} type="password" id="pwd" name="pwd"></input><br/>
       <button type="submit">Register</button>
      </form> */}
        </div>
    );
};

export default Register;